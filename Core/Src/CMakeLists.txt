# set(LIB_SRC
#     OLED_SSD1306.cpp
# )

# add_library(${PROJECT_NAME}-lib ${LIB_SRC})
# target_compile_options(${PROJECT_NAME}-lib PRIVATE ${FLAGS})
# target_link_options(${PROJECT_NAME}-lib PRIVATE -fsanitize=address -fsanitize=undefined)
